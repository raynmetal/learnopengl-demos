#version 330 core

uniform float uniColor;

in vec3 Color;

out vec4 outColor;

void main() {
    // Color output is determined by vertex
    // outColor = vec4(Color, 1.0);

    // Color output is determined by uniColor attribute
    outColor = vec4(0.0, uniColor, 0.0, 1.0);
}
